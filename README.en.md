# FreePark Cloud Simple

The canonical, bilingual project README is **[README.md](README.md)** (same format as the FreePark edge repository).

This cloud platform uses **JDK 21**, Spring Boot 3.5, Vue 3, and MySQL 8.

- Backend default port: `8080`
- Admin console: `5173`
- User portal: `5174`
- Default login: `admin` / `admin123`
